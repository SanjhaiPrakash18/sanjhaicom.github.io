document.addEventListener("DOMContentLoaded", function () {
    var video = document.getElementById('Automate-video');
    if (video) {
        video.playbackRate = 2; // Set playback rate to 2x
    }
});
document.addEventListener("DOMContentLoaded", function () {
    var video = document.getElementById('Design-video');
    if (video) {
        video.playbackRate = 2; // Set playback rate to 2x
    }
});